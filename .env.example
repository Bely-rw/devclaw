# GoClaw — Environment Variables
# Copy to .env and fill in your values:
#   cp .env.example .env
#
# .env is in .gitignore and will NEVER be committed.
# All secrets should go here, NOT in config.yaml.

# ── API Key (required) ────────────────────────────────────
# Your LLM provider API key. Supports OpenAI, Anthropic, GLM, etc.
GOCLAW_API_KEY=

# ── Alternative provider keys (optional) ──────────────────
# If GOCLAW_API_KEY is not set, these are checked as fallback:
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# ── Brave Search (for web-search skill) ──────────────────
# BRAVE_API_KEY=
